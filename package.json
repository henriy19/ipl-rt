{
  "name": "ipl-rt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node start.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
